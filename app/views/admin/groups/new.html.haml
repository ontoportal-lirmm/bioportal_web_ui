= render_in_modal do
  - @title = t('admin.groups.new.create_new_group')

  %div
    = form_for :group, url: admin_groups_path, method: "POST", remote: true, data: { collection: "groups"}, html: {class: "admin-collection-form" } do |f|
      = render partial: "form", locals: {f: f, title_text: t('admin.groups.new.create_group'),
                                               button_text: t('admin.groups.new.create_group') }
