development:
  adapter: async

test:
  adapter: test

production:
  adapter: redis
  url: <%= "redis://#{ENV.fetch("CACHE_HOST", "localhost")}:6379/1" %>
  channel_prefix: bioportal_web_ui_production
